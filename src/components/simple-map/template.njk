{% if params.title or params.zoom or params.lat or params.lng %}
  <simple-map
    title="{{ params.title }}"
    zoom="{{ params.zoom }}"
    lat="{{ params.lat }}"
    lng="{{ params.lng }}"
    enableToggle="{{ params.enableToggle | default(false) }}"
  ></simple-map>
{% endif %}
